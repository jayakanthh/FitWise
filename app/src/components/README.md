# components/ — reusable UI pieces (Pruthvi)

Small, reusable building blocks used across screens: `Button.tsx`, `Card.tsx`, `StreakBadge.tsx`, `ExerciseRow.tsx`, etc.

Rule of thumb: if you're copy-pasting the same chunk of UI into a second screen, pull it out into a component here.
