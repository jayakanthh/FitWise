import Placeholder from '../components/Placeholder';

/** Owner: Pruthvi (UI). Profile, body measurements, health notes. */
export default function ProfileScreen() {
  return <Placeholder emoji="👤" title="Profile & Health" desc="Measurements & health notes" />;
}
