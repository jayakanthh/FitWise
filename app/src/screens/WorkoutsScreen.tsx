import Placeholder from '../components/Placeholder';

/** Owner: Pruthvi (UI). Log a workout, exercise library, history. */
export default function WorkoutsScreen() {
  return <Placeholder emoji="🏋️" title="Workouts" desc="Log lifts, follow plans, track PRs" />;
}
