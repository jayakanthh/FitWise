import Placeholder from '../components/Placeholder';

/** Owner: Pruthvi (UI). Phase 2 — crew, PR leaderboard, streak leaderboard. */
export default function GroupScreen() {
  return <Placeholder emoji="👥" title="Friend Group" desc="PR & streak leaderboards, supplements" />;
}
